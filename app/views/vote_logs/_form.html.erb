<%= form_for(@vote_log) do |f| %>
  <% if @vote_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote_log.errors.count, "error") %> prohibited this vote_log from being saved:</h2>

      <ul>
      <% @vote_log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :hack_id %><br />
    <%= f.number_field :hack_id %>
  </div>
  <div class="field">
    <%= f.label :hack_score %><br />
    <%= f.text_field :hack_score %>
  </div>
  <div class="field">
    <%= f.label :logged_at %><br />
    <%= f.datetime_select :logged_at %>
  </div>
  <div class="field">
    <%= f.label :time_index %><br />
    <%= f.number_field :time_index %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
